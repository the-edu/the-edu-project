import { ColumnLayout } from '@/components/layout/column-layout';

const StudyNoteDetailContentsSection = () => {
  // const noteData = await getStudyNoteDetail(Number(id));

  return (
    <ColumnLayout.Right className="border-line-line1 h-fit rounded-xl border bg-white px-8 py-10"></ColumnLayout.Right>
  );
};

export default StudyNoteDetailContentsSection;
